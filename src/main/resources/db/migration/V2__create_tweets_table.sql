CREATE TABLE tweets (
    id SERIAL NOT NULL,
    name VARCHAR(256),
    text VARCHAR(512),
    image_url VARCHAR(256),
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
);